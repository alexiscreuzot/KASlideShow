language: objective-c
osx_image: xcode9.3

branches:
 only:
 - master

script:
- xcodebuild clean build -sdk iphonesimulator -workspace KASlideShow.xcworkspace -scheme KASlideShow CODE_SIGNING_REQUIRED=NO
